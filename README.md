# Lab 10 - Word Analyzer

This project is an object-oriented Python word count program. The user selects one of four text files from a menu, and the program analyzes the selected file by counting the frequency of each word.

This project demonstrates OOP, file I/O with pathlib, dictionaries, exception handling, string manipulation, menu-driven programming, and dependency management.